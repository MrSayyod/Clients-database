<h1>Users</h1>
<% if @users.count == 0%>
  <p>There are no users yet</p>
<% else %>
  <%= pluralize(@users.count, "user") %>
<% end%>
<ol>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user %></li>
  <% end%>
</ol>